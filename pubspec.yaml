name: kjbn_labs
description: A
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0                 #Statemanagement Framework based on Providers
  stacked_services: ^1.1.0        #Helper Services (navigation)
  flutter_secure_storage: ^9.0.0  #Shared preferences but are more secure in case device is tampered with.
  hive_flutter: ^1.1.0            #In app encrypted DB. Once a key is used to encrypt tables, Only that key can decrypt the DB.

dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true

